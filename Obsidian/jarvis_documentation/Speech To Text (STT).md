#### Uses faster-whisper
- Used to be through speaches, now uses faster-whisper quart server
#### Installation
```
cd stt
uv sync
```
#### Running
```
cd stt
source .venv/bin/activate
python main.py
```
